global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'fedmed-sre-team'

receivers:
- name: 'fedmed-sre-team'
  webhook_configs:
  - url: 'http://localhost:8000/api/v1/telemetry/alert-hook'
